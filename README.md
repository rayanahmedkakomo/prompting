# ai_toolbox

A simple Python library scaffold for the `ai_toolbox` package.

## Install

```bash
pip install ai_toolbox
```

## Development

- Install dev dependencies: `python -m pip install -U ".[dev]"`
- Run tests: `pytest`
- Format: `black .`
- Lint: `ruff .`
